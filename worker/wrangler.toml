name = "ispl-smtp"
main = "src/index.js"
compatibility_date = "2024-11-01"

# DOMINIO SCRIPT del propio worker NO se expone: el fetch viene desde el worker como mto al sitio.
# El sitio pone en un secreto la URL del worker (WORKER_URL en js/main.js).
