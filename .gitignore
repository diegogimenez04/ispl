# Windows descarga por internet (streams ADS)
*:Zone.Identifier

# Secretos locales de Wrangler (NUNCA subir)
.dev.vars
.dev.vars.*
!.dev.vars.example

# Caché/estado local de Wrangler
.wrangler/
node_modules/

# Documentos fuente para RAG (no subir)
rag/

# Videos originales sin comprimir (los comprimidos viven en assets/demos/)
assets/videos/
